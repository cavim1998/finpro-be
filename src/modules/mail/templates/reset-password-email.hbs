<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Your Password</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        line-height: 1.6;
        color: #2f4f4f;
        margin: 0;
        padding: 0;
        background-color: #eaf7f9;
      }

      .container {
        max-width: 600px;
        margin: 40px auto;
        padding: 24px;
        background-color: #ffffff;
        border-radius: 16px;
        box-shadow: 0 8px 24px rgba(29, 172, 188, 0.15);
      }

      .header {
        background-color: #1dacbc;
        color: #ffffff;
        padding: 36px 20px;
        text-align: center;
        border-radius: 16px 16px 0 0;
        margin: -24px -24px 28px -24px;
      }

      .header h1 {
        margin: 0;
        font-size: 26px;
        font-weight: 700;
      }

      .icon {
        font-size: 46px;
        margin-bottom: 10px;
      }

      .content {
        padding: 6px 6px;
        text-align: center;
      }

      .content p {
        margin: 14px 0;
        font-size: 16px;
        color: #2f4f4f;
      }

      .button {
        display: inline-block;
        background-color: #1dacbc;
        color: #ffffff !important;
        padding: 14px 36px;
        text-decoration: none;
        border-radius: 999px;
        font-weight: 600;
        margin: 22px 0;
        font-size: 16px;
      }

      .code {
        background-color: #f3fbfc;
        padding: 16px;
        border-radius: 10px;
        border: 1px dashed #1dacbc;
        margin: 22px 0;
        word-break: break-all;
        font-family: monospace;
        font-size: 14px;
        color: #1dacbc;
      }

      .warning {
        background-color: #fff7e6;
        border: 1px solid #ffd591;
        color: #8c6d1f;
        padding: 14px;
        border-radius: 10px;
        margin: 24px 0;
        font-size: 14px;
        text-align: left;
      }

      .expiry {
        color: #e5533d;
        font-size: 14px;
        margin-top: 16px;
      }

      .footer {
        text-align: center;
        color: #6b8f94;
        font-size: 12px;
        margin-top: 36px;
        padding-top: 20px;
        border-top: 1px solid #e0f1f4;
      }
    </style>
  </head>
  <body>

    <div class="container">

      <div class="header">
        <h1>Reset Your Password</h1>
      </div>

      <div class="content">
        <p>Hello {{fullName}},</p>

        <p>
          We received a request to reset your account password. Click the button
          below to set a new one.
        </p>

        <a href="{{resetLink}}" class="button">
          Reset Password
        </a>

        <p>Or copy and paste this link into your browser:</p>

        <div class="code">{{resetLink}}</div>

        <div class="warning">
          ⚠️
          <strong>Security Notice</strong><br />
          If you did not request a password reset, you can safely ignore this
          email. Your account will remain secure.
        </div>

        <p class="expiry">⏰ This link will expire in 1 hour.</p>
      </div>

      <div class="footer">
        <p>&copy; 2026 LaundryQ. All rights reserved.</p>
        <p>This is an automated email. Please do not reply.</p>
      </div>

    </div>

  </body>
</html>